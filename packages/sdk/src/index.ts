/**
 * @bonfire/sdk — the typed client layer over @bonfire/core's published surface.
 *
 * Operator-prep scaffold: the BF-08 maker replaces this placeholder with the
 * generated client, the session boundary (authenticate → BonfireSession), and
 * the reactive useClinicalQuery store.
 */
export type { BonfireError, Result } from "@bonfire/core";
