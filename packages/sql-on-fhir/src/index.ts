/**
 * @bonfire/sql-on-fhir — SQL-on-FHIR v2 ViewDefinition runner (BF-04).
 *
 * Operator prep scaffold: the public surface is authored by the slice maker.
 * One pure projection engine serves both the in-memory conformance runner
 * (`bun run conformance`) and the Postgres vd_* materializer
 * (`bun run projections:rebuild`).
 */
export {};
